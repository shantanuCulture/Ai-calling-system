# Backend API origin — set this when frontend and backend are on different domains.
# Leave blank in local development (Vite proxy handles /api → localhost:3001).
# Example for production:
VITE_API_BASE_URL=https://api.cultureholidays.com
